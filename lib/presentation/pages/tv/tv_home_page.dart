import 'package:flutter/material.dart';

class TvHomePage extends StatelessWidget {
  static const routeName = '/tv-home-page';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('aww'),
    );
  }
}
